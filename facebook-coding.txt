  public class Resolution{
    
    public int getNoZeorElement(int[] array ){
      
      int len = array.length;
      int j = array.length;
      int i = 0;
      
      for(i = 0; i < len; i ++){
     
        num ++;
        
        if(array[i] == 0){
          
          int tmp = i;
        
          while(array[i] == 0){
            i ++;
            
          }
          
          int right = i;
          int tmpLen = (right -tmp);
          
          for(int j = 0; j < tmpLen; j ++  ){
            array[tmp + j] = array[right - j];
            
          }
   
        }
        
        
      }
    
    return i;
    
    
    }
    
  }
  
    
public boolean isPalindrome(String input) {
  
  int right = input.lenght();
  int left = 0;
  
  
  while(left <= right){
    
    char leftChar = input.charAt(left);
    char rightChar = input.charAt(right);
    
    if(!isChar(leftChar)){
      left ++;
      continue;
    }
    if(!isChar(rightChar)) {
      right --;
      continue;
    }
    
    if(isChar(leftChar) && isChar(riughtChar)){
      char lowLeft = toLower(leftChar);
      char lowRight = toLower(rightChar);
      
      if(lowLeft == lowRight){
        
        left ++;
        right --;
      }
      else{
        return false;
      }
    
    }
    
    
    
  }
  
  return true;
}

public boolean isChar(char c);
public char toLower(char c);